# Backport ledger · sabigoku → zigoku

| Field | Value |
|---|---|
| Purpose | Cases where port work fixed, hardened, or questioned something zigoku still carries at `083abd3`. The work-list for the eventual backport pass. |
| Rules | Add a row the moment a deviation is ratified or a review finding is confirmed against the freeze. Rows cite chapters instead of re-explaining. No Zigoku-module tickets until backporting actually starts; this doc becomes the filing checklist then. |

| Case | zigoku @ freeze | sabigoku | Verdict | zigoku site | Source |
|---|---|---|---|---|---|
| K-2 forced-preferred walk dead-end | Stale-stamp re-route onto a search-only preferred provider exhausts a single-provider walk and strands the grid while bindings exist | Walk-origin tag split; K-2 law in 03 §5.3 | **Backport** | `tui/resolve_state.zig`, `tui/app.zig` | 07 K-2, ROD-430 |
| Non-sync 429 classification | Any non-200 collapses to no-answer; hammering risk called out in 06 §8b | Distinct rate-limited error class, no client retry | **Backport** | `anilist.zig` `postGql` | ROD-435 |
| Control-char strip coverage | `stripControls` drops C0 + DEL only; C1, bidi overrides, zero-width pass into terminal cells (RTL title spoofing reproduced) | Widened filter: C1 + U+202A-2E + U+2066-69 + U+200B-D + U+FEFF | **Backport** | `anilist.zig` `stripControls` | ROD-435 chaos pass |
| HTTP redirect handling on catalog POSTs | `std.http.Client` redirect behavior unaudited; a followed redirect can re-route the request off-host | Redirects refused on the catalog client | **Needs check** first: confirm what zig std does at freeze, then decide | `anilist.zig` `fetchGql` | ROD-435 chaos pass |
| Unterminated `<` in description | `sanitizeDescription` never leaves tag state; rest of the synopsis silently vanishes | Same (CLONE kept for parity) | **Needs decision**; fix would apply to both | `anilist.zig` `sanitizeDescription` | ROD-435 chaos pass |
| Cover URL sanitizing asymmetry | `thumb` is the one API string with no control-strip ("validated on fetch") | `cover_url` stripped like every sibling field | **Backport** (cheap, closes the asymmetry) | `anilist.zig` `mediaToMeta` | ROD-435 chaos pass |
| Blank-romaji title guard | Seed-flag gate (ROD-312) protects canonical titles healed off provider seeds | NULLIF-at-merge; seed flag not ported (no provider seeds reach these writers) | **Skip**: zigoku's gate fits its provider-seeded identity model; ours fits AniList-first | `anilist.zig`, `store.zig` | 02 §merge, ROD-434 |
| genres/studios encoding | `'\n'`-joined TEXT | JSON string arrays (02 L5) | **Skip**: encoding choice serves each language's tooling; no product difference | `store.zig` | 02 L5, ROD-434 |
| IPv6 SSRF: compatible-address gap | `isPrivateV6` checks only the `::ffff:` mapped prefix, so `::169.254.169.254` (legacy `::a.b.c.d` compatible form) reaches cloud metadata | `to_ipv4` covers mapped AND compatible; `::/96` folds into `0/8` | **Backport** (ratified ROD-436): one-line fix, closes a live SSRF class | `util/fetchguard.zig` `isPrivateV6` | ROD-436 chaos pass |
| Final stream/cover URL not fetch-guarded | `followProvider`/`consider`/`coverRequest` guard only the intermediate GETs; the `StreamLink.url` handed to mpv and the absolute cover URL never pass `guardFetchUrl`, though 03 §6.7 reads as if they should | Same gap today; guard deferred to the fetch/launch sites (ROD-437 mpv spawn, ROD-438 cover fetch) | **Needs decision** for both codebases: guard at construction vs at the fetch site; sabigoku plan is fetch-site | `providers/allanime.zig`, `tui/*` launch path | ROD-436 chaos pass |
| Tier-B mined-id hostile forgery | `anilistIdFromThumb` mines an AniList id from an attacker-controlled thumb; `bestIdMatch` has no title floor, so a forged thumb + matching eps/year binds an arbitrary provider id to the victim show | Same trust model (faithful port); comment made honest about what the veto does NOT cover | **Skip** (design): tier-B id agreement binds without a title floor by ROD-342; a compromised provider can already mis-serve. Documented, not a fixable gap without changing the tier contract | `resolver.zig` `bestIdMatch`, `providers/allanime.zig` | ROD-436 chaos pass |
| Provider search titles not control-stripped | `stripControls` runs only on AniList enrichment strings; senshi/megaplay/allanime search titles skip it, so a hostile title (RTL override, OSC injection) could reach a terminal cell | Same gap (faithful); in sabigoku provider titles are tier-C binding-only (03 §1) and discarded before render, but that assumes the not-yet-built TUI never surfaces them | **Needs check** at ROD-439 (TUI shell): confirm provider titles never reach ratatui; if they do, strip at map or render. Both codebases share the gap | `senshi.zig`/`allanime.zig` `mapAnime`/`edgeToAnime` | ROD-441 chaos pass |
| Final softsub `sub_url` not fetch-guarded | `pickSubtitle`/`fetch_subtitle` argv-vet the picked track (`cleanArg`) but never `guardFetchUrl` it; the sidecar/probe GETs are guarded, the URL handed to mpv `--sub-file` is not, so a `default`/first/English track aimed at loopback or cloud metadata reaches mpv. Same shape in senshi + megaplay | Guards the final pick in both providers (drop-and-play-raw on failure): megaplay `map_sources`, senshi `fetch_subtitle` | **Backport** (ratified ROD-445): the softsub path is the one provider URL that bypasses the stream proxy; guard it at the pick | `providers/megaplay.zig` `pickSubtitle`, `providers/senshi.zig` sub path | ROD-445 chaos pass |
| Proxy client-facing socket has no timeout | zigoku's `proxy.zig` sets no per-socket read/write timeout (std.Io has none); a stalled local client pins a handler thread + its state for the port's lifetime, and `stop()` only joins the accept thread. Bounded teardown exists (`Gate` drain cap) but the pin itself does not | `set_read_timeout` + `set_write_timeout` per connection, and a `MAX_HEAD_BYTES` cap on the request head (std affordances) | **Needs check**: zig std.Io lacks a per-socket read timeout at freeze, so the fix needs a `withDeadline`-style wrap on the client read, not a flag; different mechanism, same goal | `proxy.zig` `handleConn` | ROD-445 chaos pass |
| `joinUrl` mishandles protocol-relative `//host` | `//host/path` is treated as an absolute-path-on-current-host, producing a malformed URL instead of resolving to the new host. Byte-identical blind spot in both codebases; benign in playlist rewriting, but `fetch_upstream`'s redirect-Location resolution is a new, security-relevant call site with no test at that shape | Same gap (faithful port) | **Needs check** whenever `join_url` is next touched: decide protocol-relative handling; add a redirect-Location test at that shape | `providers/hls.zig` `joinUrl` | ROD-445 chaos pass |
| `is_control()`-style strip misses the Cf class | zigoku's cell/label sanitizers (and the `--force-media-title` clean) drop Cc only; bidi overrides (U+202E), isolates, and zero-width chars are Cf and pass through. vaxis/terminal cells may or may not backstop them; mpv's window title certainly does not | All render-edge strips route through `domain::strip_controls` (the ROD-435 list: C0+DEL+C1+bidi+isolates+zero-width); Cf-class tests pin cell_text, the mpv title, and the ratatui buffer backstop that guards unfiltered list titles | **Needs check** in zigoku: audit every `is_control`-shaped filter (cell render, media-title) against the Cf class; the ROD-435 list already exists in both codebases' AniList mappers, reuse it | `tui/render.zig` cells, `player.zig` media-title | ROD-439 final-gate chaos pass |
| Auto-import WATCHING/REPEATING on sync (O3) | Unmatched AniList list entries are counted only, never imported (O3 lean No); reconcile scoped to rows already in the library | WATCHING/REPEATING unmatched entries auto-import as watchlist rows (add-only, no removal edge); collection query widened for `title`/`episodes` so imports land usable with no extra fetch. O3 amended No to Yes (filtered) in 06 | **Needs decision** (product, not a fix): backport only if zigoku wants the same pull-in-active UX; add-only stance and the widened-query enrichment source carry over if it does | `anilist.zig` list-collection query, `store.zig` reconcile | 06 §5.5/§11 O3, ROD-467 |
| Search-derived bind can steal a `(provider, provider_id)` pair cross-show | `bindCanonical` collision handling assumes "same show moved," never "two shows collided"; a search-derived bind (fallback walk, ROD-351 prewarm) that answers a pair another show already owns silently unbinds the bystander. Unattended prewarm runs this at library scale | Prewarm's Found mint refuses a pair another show owns (`show_id_for_binding` guard, read errors fail closed); the user-facing walk deliberately keeps freeze semantics pending a store-layer decision | **Needs check** in zigoku (what does `bindCanonical` do on collision?) and **needs decision** both codebases: ownership guard at the store layer for every search-derived mint, plus a shared `provider_id` shape guard (today each provider self-guards individually; safety is incidental) | `store.zig` `bindCanonical`, `resolve.zig` search mints | ROD-449 chaos pass |
| Sync merge pushes a corrected progress back over the correction | `progress = max(local, remote)` in every merge cell. Fix a too-high entry on AniList and the pull keeps the stale local value while re-baselining the snapshot to the corrected remote; the mismatch then queues the row and the next push overwrites the correction. The play ratchet and the union raise are both max, so the local paths that can lower it are `recomputeProgress` (`store.zig:1538`, unconditional), the `setListStatus` completed snap (`store.zig:1484`), and `restoreListStatus` (`store.zig:1511`, the undo restore), each a deliberate user gesture behind a keypress. A wrong-high value imported by a pull is therefore unreachable by any automatic path | Progress runs the status matrix against the snapshot's progress half: remote-only moves adopt whole (downward included), local-only keeps local, both-moved still maxes | **Backport** (ratified ROD-497): silent destruction of a user edit on the user's own account, and the raise-only rule buys nothing the both-moved cell does not already cover | `store.zig` reconcile | 06 §5.4, ROD-497 |
| Duplicate list entries deduped by wire order | `MediaListCollection` can return one row per group a media belongs to. Whether those copies can carry divergent progress is unevidenced either way here, so the rule is defensive: it fixes what happens IF they diverge. zigoku dedupes at ingest with an unconditional last-wins overwrite (`anilist.zig:605-611`), so the adopted pair is whichever group the server happened to serialize last. Arbitrary, and unstable across responses, but not systematically biased | Collapse by `(updatedAt, progress)`: recency decides, progress only breaks a tie, so the fold is order-independent even when the whole group is unstamped. Query widened to fetch `updatedAt`, which zigoku never requested (`anilist.zig:560`) | **Needs check**: last-wins is not a correctness bug the way sabigoku's own pre-ROD-497 max-progress collapse was, so this is a robustness improvement to consider, not a defect to repair. Backporting costs a query field | `anilist.zig` MLC query + dedupe loop | 06 §5.4, ROD-497 |
| Merge can synthesize completed with partial progress | zigoku's reconcile is `@max` and nothing else (`sync.zig:198-214`), so its status and progress outcomes cannot disagree in a way that invents a pair. Latent rather than absent: it is the raise-only rule that hides it | Cross-half floor raises progress to local's own value when a kept-local `completed` meets an adopted-remote progress behind it, and only when the remote did not itself hold a completed pair. Deliberately holds a value local already had; raising to the episode total instead mints watch data on any completed row whose cached total drifted, pushes it, and re-mints it every pull | **Do not backport**: the synthesis only exists because sabigoku merges the halves independently. zigoku has no such split and would gain the hazard, not lose one | `sync.zig` reconcile | 06 §5.4, ROD-497 |
| Frontier stamp fires on any progress change | No equivalent: zigoku has no `progress_stamped_at` and no recency-anchored resume at all. Its `resumeSeed` is frontier-anchored, which is why ROD-477 built the stamp in the first place. It is not raise-only either, contrary to an earlier claim in this row: `recomputeProgress` writes `progress = ?` unconditionally (`store.zig:1539-1541`) and `setListStatus` snaps `= t` (`store.zig:1487-1491`), which lowers on a rewatch past the total. Both are harmless there precisely because no stamp exists to corrupt | Stamps on a rise only in the sync path; `recompute` still stamps both ways, since resetting from local rows is a request to drop the markers | **Skip**: sabigoku-only mechanism, so there is nothing to backport and nothing to check. Recorded because the bug was live in sabigoku and the reasoning does not transfer | n/a | 05 §10.7, ROD-497 |
